import React from 'react';

const Description = ({ text }) => {
  return (
    <div>
      <p>{text}</p>
      {/* Aquí podrías agregar más detalles del producto, como características técnicas */}
    </div>
  );
};

export default Description;